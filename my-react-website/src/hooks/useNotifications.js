import React from 'react';
import useNotifications from './useNotifications'; // Adjust the import path based on your folder structure

const MyComponent = () => {
  useNotifications();

  // You can render UI components here
  return (
    <div>
      {/* Render UI components */}
    </div>
  );
};

export default MyComponent;
